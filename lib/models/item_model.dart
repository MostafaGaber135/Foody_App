class ItemModel {
  final String image, title, subTitle;
  final double rating;
  ItemModel({
    required this.image,
    required this.title,
    required this.subTitle,
    required this.rating,
  });
}
